/// @description Menu da batalha

if (room == rm_batalha_fora)
{
	/*
	// desenhando caixa do menu
	draw_rectangle_color(0, room_height, room_width, room_height - 60, c_teal, c_teal, c_teal, c_teal, false);
	draw_rectangle_color(0, room_height, room_width, room_height - 60, c_black, c_black, c_black, c_black, true);
 
	// movendo pela posicao
	var maximo = ds_list_size(global.inimigo_batalha) - 1;

	if (keyboard_check_pressed(vk_up))  posicao --;
	if (keyboard_check_pressed(vk_down))  posicao ++;
	
	if (posicao < 0) posicao = maximo;
	if (posicao > maximo) posicao = 0;
	
	// desenhando o circulo
	draw_circle_color(
		ds_list_find_value(global.inimigo_batalha, posicao).x - 60,
		ds_list_find_value(global.inimigo_batalha, posicao).y - 20,
		10, c_red, c_maroon, false
	);
	*/
}

